ExamBuilder GR v1.0.0 stable release patch

Apply over v0.7.7 RC8, replace all files, delete ExamBuilderGR\bin, ExamBuilderGR\obj and dist, then Build -> Rebuild Solution.
For the existing GitHub repository, copy the patch files over it and run scripts\release-v1.ps1.
